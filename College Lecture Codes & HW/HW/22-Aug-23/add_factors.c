#include <conio.h>
#include <stdio.h>
void main()
{
    int n, sum;

    printf("Enter a number: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++)
    {
        if (n % i == 0)
        {
            sum = sum + i;
        }
    }

    sum of factors = % d ", sum);

                     getch();
}